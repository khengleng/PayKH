# paykh/sdk-php — PHP SDK (Phase 4)

Placeholder. The official PHP SDK ships in **Phase 4**. Until then, use the REST
API directly — see [`docs/api.md`](../../docs/api.md) and the webhook
verification example in [`docs/webhooks.md`](../../docs/webhooks.md).
