{
  "name": "@paykh/security",
  "version": "0.1.0",
  "private": true,
  "description": "Cryptographic primitives: API key hashing, HMAC webhook signing, AES-GCM encryption, id generation",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.1.2",
    "typescript": "^7.0.2"
  }
}
