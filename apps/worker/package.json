{
  "name": "@paykh/worker",
  "version": "0.1.0",
  "private": true,
  "description": "PayKH background worker (webhook delivery, expiry sweeps) — Phase 2",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node src/main.ts",
    "start": "node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@paykh/shared-types": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
